{
  "name": "orbitguard-fire-backend",
  "version": "0.1.0",
  "private": true,
  "description": "OrbitGuard Fire MVP backend scaffold",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "dev": "node --enable-source-maps -r ts-node/register src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
